# tibrezus-agents

## 0.1.0

### Minor Changes

- Initial skills.sh-distributed skill set. The repo is versioned with Changesets
  and distributed via `npx skills@latest add tibrezus/agents` (no npm registry
  publish). Includes a maintainer `scripts/link-skills.sh` for local dev linking
  into `~/.agents/skills` and `~/.claude/skills`.
